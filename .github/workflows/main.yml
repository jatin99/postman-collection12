name: Postman Tests Workdflow
on:
  push: 
    branches: 
      - main
  workflow_dispatch:
    inputs: 
      environment:
        description: 'Choose Env'
        required: true
        default: staging
  
  
